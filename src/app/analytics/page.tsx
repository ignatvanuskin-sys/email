"use client";
import { useEffect, useState } from "react";
import { api } from "@/lib/client";
import { useToast } from "@/components/Toast";
import Link from "next/link";

type Analytics = { totals: { sent: number; failed: number; bounced: number; unsubscribed: number; replies: number; conversions: number; revenue: number }; rates: { replyRate: number; conversionRate: number }; cohorts: Array<{ cohort: string; contacts: number; active: number; purchases: number; revenue: number; retentionRate: number; revenuePerContact: number }>; usage: { plan: string; period: string } };
export default function AnalyticsPage() { const { notify } = useToast(); const [analytics, setAnalytics] = useState<Analytics | null>(null); const [recommendations, setRecommendations] = useState<{ summary: string; recommendations: string[] } | null>(null); useEffect(() => { api<{ analytics: Analytics }>("/api/analytics/summary").then((result) => setAnalytics(result.analytics)).catch((error) => notify(error instanceof Error ? error.message : "Analytics failed", "error")); }, [notify]); const loadRecommendations = async () => { try { const result = await api<{ recommendations: typeof recommendations }>("/api/analytics/recommendations", { method: "POST" }); setRecommendations(result.recommendations); } catch (error) { notify(error instanceof Error ? error.message : "Recommendations failed", "error"); } }; if (!analytics) return <div className="empty">Loading analytics...</div>; const totals = analytics.totals; return <div><div className="page-head"><div><h1 className="page-title">Analytics</h1><p className="page-sub">Cross-campaign performance, cohorts and revenue.</p></div><Link href="/" className="btn">Dashboard</Link></div><div className="metric-grid"><Metric label="Sent" value={totals.sent} /><Metric label="Replies" value={totals.replies} /><Metric label="Conversions" value={totals.conversions} /><Metric label="Revenue" value={totals.revenue.toFixed(2)} /><Metric label="Reply rate" value={`${analytics.rates.replyRate}%`} /><Metric label="Conversion rate" value={`${analytics.rates.conversionRate}%`} /></div><section className="card" style={{ padding: 18, marginTop: 20 }}><div className="row"><div className="section-label grow">AI optimization recommendations</div><button className="btn btn-primary btn-sm" onClick={loadRecommendations}>Analyze</button></div>{recommendations && <><p className="small">{recommendations.summary}</p><ul className="small">{recommendations.recommendations.map((item) => <li key={item}>{item}</li>)}</ul></>}</section><section className="card" style={{ padding: 18, marginTop: 20 }}><div className="row"><div className="section-label grow">Cohorts and revenue</div><span className="badge blue">{analytics.usage.plan} · {analytics.usage.period}</span></div>{analytics.cohorts.length === 0 ? <div className="empty">No cohort events yet.</div> : <div className="stack" style={{ gap: 7, marginTop: 10 }}>{analytics.cohorts.map((cohort) => <div className="row small" key={cohort.cohort}><span className="grow">{cohort.cohort}</span><span>{cohort.contacts} contacts</span><span>{cohort.retentionRate}% retention</span><span>{cohort.purchases} purchases</span><span>{cohort.revenue.toFixed(2)} revenue</span><span>{cohort.revenuePerContact.toFixed(2)}/contact</span></div>)}</div>}</section></div>; }
function Metric({ label, value }: { label: string; value: number | string }) { return <div className="kpi"><div className="label">{label}</div><div className="value">{value}</div></div>; }
